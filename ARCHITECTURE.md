# Архитектура ViCord

## 📐 Обзор архитектуры

ViCord построен на основе современной компонентной архитектуры React с использованием TypeScript для обеспечения типобезопасности.

## 🏗️ Структура компонентов

### Иерархия компонентов

```
App (AppProvider)
└── MainLayout
    ├── ServerList
    ├── ChannelList
    └── ChatArea
```

### Детальное описание компонентов

#### 1. **App Component**
- Корневой компонент приложения
- Оборачивает все приложение в `AppProvider` для глобального состояния
- Рендерит `MainLayout`

#### 2. **MainLayout**
- Основной layout приложения
- Использует Flexbox для трехколоночной структуры
- Управляет анимациями входа с помощью Framer Motion

#### 3. **ServerList**
- Отображает список серверов в левом сайдбаре
- Функции:
  - Кнопка "Домой"
  - Список серверов с иконками
  - Кнопка добавления сервера
  - Индикатор активного сервера
  - Tooltip при наведении

#### 4. **ChannelList**
- Показывает каналы текущего сервера
- Функции:
  - Заголовок сервера с dropdown
  - Категории каналов (текстовые, голосовые)
  - Список каналов с иконками
  - Панель пользователя внизу
  - Индикатор статуса

#### 5. **ChatArea**
- Главная область чата
- Функции:
  - Заголовок канала
  - Область сообщений с прокруткой
  - Поле ввода с кнопками
  - Анимация появления сообщений

## 🔄 Управление состоянием

### AppContext

Централизованное управление состоянием через React Context API:

```typescript
interface AppContextType {
  currentServer: Server | null
  setCurrentServer: (server: Server | null) => void
  currentChannel: Channel | null
  setCurrentChannel: (channel: Channel | null) => void
  servers: Server[]
  channels: Channel[]
  messages: Message[]
  currentUser: User
}
```

### Поток данных

1. **Состояние** хранится в `AppContext`
2. **Компоненты** подписываются через `useApp()` hook
3. **Изменения** распространяются автоматически через Context
4. **Реактивность** обеспечивается React

## 📊 Модель данных

### Основные типы

```typescript
// Сервер
interface Server {
  id: string
  name: string
  icon?: string
  iconColor?: string
}

// Канал
interface Channel {
  id: string
  name: string
  type: 'text' | 'voice'
  serverId: string
}

// Сообщение
interface Message {
  id: string
  content: string
  author: User
  timestamp: Date
  channelId: string
}

// Пользователь
interface User {
  id: string
  username: string
  avatar?: string
  status: 'online' | 'idle' | 'dnd' | 'offline'
}
```

## 🎨 Стилизация

### Tailwind CSS

Используется utility-first подход:
- Кастомная цветовая палитра Discord
- Responsive design
- Hover и focus состояния
- Кастомные анимации

### Framer Motion

Анимации для:
- Переходы между состояниями
- Hover эффекты
- Layout анимации
- Появление элементов

## 🔌 Будущие расширения

### Backend Integration

```
Frontend (React)
    ↓
REST API / GraphQL
    ↓
Backend (Node.js + Express)
    ↓
Database (PostgreSQL)
```

### WebSocket для Real-time

```
Client ←→ Socket.io ←→ Server
```

### Authentication Flow

```
Login → JWT Token → Authenticated Requests → Protected Routes
```

## 🧩 Паттерны и практики

### 1. **Component Composition**
Компоненты разбиты на небольшие переиспользуемые части.

### 2. **Custom Hooks**
```typescript
const { currentServer, channels } = useApp()
```

### 3. **TypeScript First**
Строгая типизация для предотвращения ошибок.

### 4. **Utility Functions**
Вспомогательные функции в `lib/utils.ts`:
- `cn()` - объединение классов
- `formatTime()` - форматирование времени
- `formatDate()` - форматирование даты

### 5. **Separation of Concerns**
- Components - UI логика
- Contexts - Состояние
- Lib - Утилиты и типы
- Layouts - Структура страниц

## 🚀 Производительность

### Оптимизации

1. **React.memo** для предотвращения лишних рендеров
2. **useCallback** для мемоизации функций
3. **useMemo** для вычисляемых значений
4. **Code splitting** через lazy loading
5. **Vite** для быстрой сборки

### Лучшие практики

- Избегание глубокой вложенности
- Минимизация пропсов
- Эффективное использование Context
- Оптимизация изображений
- Lazy loading компонентов

## 📱 Масштабируемость

### Горизонтальное масштабирование

Добавление новых features:
```
src/
├── features/
│   ├── voice/
│   ├── video/
│   └── bots/
```

### Вертикальное масштабирование

Улучшение существующих компонентов:
- Виртуализация списков
- Бесконечная прокрутка
- Оптимизация рендеринга

## 🔐 Безопасность (Планируется)

- XSS защита
- CSRF токены
- JWT authentication
- Rate limiting
- Input validation
- Sanitization

## 🧪 Тестирование (Планируется)

- Unit тесты - Jest
- Integration тесты - React Testing Library
- E2E тесты - Playwright
- Visual regression - Chromatic

---

Эта архитектура обеспечивает:
- ✅ Масштабируемость
- ✅ Поддерживаемость
- ✅ Производительность
- ✅ Типобезопасность
- ✅ Developer Experience

