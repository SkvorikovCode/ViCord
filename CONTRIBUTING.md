# 🤝 Руководство по внесению вклада в ViCord

Спасибо за интерес к развитию ViCord! Мы рады любому вкладу.

## 📋 Оглавление

- [Кодекс поведения](#кодекс-поведения)
- [С чего начать](#с-чего-начать)
- [Процесс разработки](#процесс-разработки)
- [Структура коммитов](#структура-коммитов)
- [Стиль кода](#стиль-кода)
- [Тестирование](#тестирование)

## 📜 Кодекс поведения

Участвуя в проекте, вы соглашаетесь соблюдать наш Кодекс поведения:
- Уважительное общение
- Конструктивная критика
- Приоритет общих интересов проекта

## 🚀 С чего начать

### 1. Найдите задачу

Посмотрите Issues с метками:
- `good first issue` - для новичков
- `help wanted` - нужна помощь
- `bug` - исправление ошибок
- `enhancement` - новые фичи

### 2. Форкните репозиторий

```bash
# Форкните через GitHub UI, затем:
git clone https://github.com/YOUR_USERNAME/vicord.git
cd vicord
```

### 3. Установите зависимости

```bash
npm install
```

### 4. Создайте ветку

```bash
git checkout -b feature/amazing-feature
# или
git checkout -b fix/bug-description
```

## 🔄 Процесс разработки

### 1. Разработка

```bash
# Запустите dev сервер
npm run dev

# Проверяйте линтер
npm run lint
```

### 2. Тестирование

Убедитесь что:
- [ ] Код компилируется без ошибок
- [ ] Линтер не выдает предупреждений
- [ ] UI отображается корректно
- [ ] Нет console.log в production коде

### 3. Коммит изменений

```bash
git add .
git commit -m "feat: добавить новую фичу"
```

### 4. Push и Pull Request

```bash
git push origin feature/amazing-feature
```

Затем создайте Pull Request через GitHub UI.

## 📝 Структура коммитов

Используем [Conventional Commits](https://www.conventionalcommits.org/):

### Типы коммитов

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - документация
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `perf:` - улучшение производительности
- `test:` - добавление тестов
- `chore:` - рутинные задачи

### Примеры

```bash
feat: добавить голосовые каналы
fix: исправить отображение аватаров
docs: обновить README
style: форматировать код по ESLint
refactor: переписать ChatArea на хуки
perf: оптимизировать рендеринг списка серверов
test: добавить тесты для ServerList
chore: обновить зависимости
```

## 🎨 Стиль кода

### TypeScript

```typescript
// ✅ Хорошо
interface User {
  id: string
  username: string
  status: 'online' | 'idle' | 'dnd' | 'offline'
}

const getUser = (id: string): User | null => {
  // ...
}

// ❌ Плохо
const getUser = (id) => {
  // ...
}
```

### React Components

```typescript
// ✅ Хорошо - функциональный компонент
import { motion } from 'framer-motion'
import { cn } from '@/lib/utils'

interface ButtonProps {
  children: React.ReactNode
  onClick?: () => void
  variant?: 'primary' | 'secondary'
}

const Button: React.FC<ButtonProps> = ({ 
  children, 
  onClick, 
  variant = 'primary' 
}) => {
  return (
    <motion.button
      whileHover={{ scale: 1.05 }}
      onClick={onClick}
      className={cn(
        'px-4 py-2 rounded',
        variant === 'primary' && 'bg-discord-blue',
        variant === 'secondary' && 'bg-discord-gray'
      )}
    >
      {children}
    </motion.button>
  )
}

export default Button
```

### CSS/Tailwind

```tsx
// ✅ Хорошо - используем Tailwind utilities
<div className="flex items-center gap-2 px-4 py-2 bg-discord-dark rounded-lg">
  <span className="text-discord-text">Hello</span>
</div>

// ❌ Плохо - избегаем inline styles
<div style={{ display: 'flex', padding: '8px' }}>
  <span style={{ color: '#dbdee1' }}>Hello</span>
</div>
```

## 🧪 Тестирование

### Чек-лист перед PR

- [ ] `npm run dev` работает без ошибок
- [ ] `npm run build` успешно собирает проект
- [ ] `npm run lint` не выдает ошибок
- [ ] Нет TypeScript ошибок
- [ ] UI выглядит правильно во всех состояниях
- [ ] Код отформатирован
- [ ] Коммиты следуют соглашению

## 📋 Pull Request Guidelines

### Описание PR

Используйте этот шаблон:

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Исправление бага
- [ ] Новая функциональность
- [ ] Критическое изменение
- [ ] Документация

## Чек-лист
- [ ] Код следует стилю проекта
- [ ] Проведено self-review
- [ ] Добавлены комментарии где нужно
- [ ] Обновлена документация
- [ ] Нет новых warnings
- [ ] Добавлены тесты (если применимо)

## Screenshots (если применимо)
```

### Review Process

1. Автоматические проверки должны пройти
2. Требуется одобрение maintainer'а
3. Конфликты должны быть разрешены
4. История коммитов должна быть чистой

## 🎯 Приоритетные направления

Мы особенно приветствуем помощь в:

### Frontend
- 🎨 Улучшения UI/UX
- ⚡ Оптимизация производительности
- 📱 Адаптивный дизайн
- ♿ Accessibility

### Backend (в будущем)
- 🔌 WebSocket интеграция
- 🔐 Аутентификация
- 💾 База данных
- 📡 REST API

### Документация
- 📚 Руководства
- 💡 Примеры использования
- 🌍 Переводы
- 📹 Видео туториалы

## 💡 Идеи для вклада

### Легкие задачи
- Исправление опечаток
- Улучшение документации
- Добавление комментариев
- Мелкие UI улучшения

### Средние задачи
- Новые компоненты
- Улучшение существующих фич
- Оптимизация производительности
- Добавление анимаций

### Сложные задачи
- Backend интеграция
- WebSocket реализация
- Голосовые/видео каналы
- Настройка Tauri

## 🆘 Нужна помощь?

- 💬 Discord сервер: [пригласительная ссылка]
- 📧 Email: support@vicord.dev
- 📝 GitHub Issues: [создать issue](https://github.com/yourusername/vicord/issues)

## 📄 Лицензия

Внося свой вклад, вы соглашаетесь, что ваш код будет лицензирован под той же лицензией, что и проект.

---

Спасибо за ваш вклад! 🎉

