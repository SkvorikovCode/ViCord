# 🚀 Quick Start Guide - ViCord

## Быстрый старт за 5 минут

### 1️⃣ Установка зависимостей

```bash
# Frontend
npm install

# Backend
cd server
npm install
cd ..
```

### 2️⃣ Настройка Backend

```bash
cd server

# База данных уже настроена (.env создан)
# Запустить миграции
npm run prisma:migrate

# Заполнить базу тестовыми данными
npm run prisma:seed

cd ..
```

### 3️⃣ Запуск приложения

#### Вариант A: Автоматический запуск (Windows)
```bash
start-dev.bat
```

#### Вариант B: Ручной запуск (два терминала)

**Терминал 1 - Backend:**
```bash
cd server
npm run dev
```

**Терминал 2 - Frontend:**
```bash
npm run dev
```

### 4️⃣ Открыть в браузере

- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:3001
- **Health Check:** http://localhost:3001/health

### 5️⃣ Тестовые данные

После выполнения `npm run prisma:seed` у вас будут доступны:

**Тестовые пользователи:**
| Email | Password | Роль |
|-------|----------|------|
| user@vicord.dev | password123 | Пользователь |
| bot@vicord.dev | password123 | Бот |
| admin@vicord.dev | password123 | Администратор |

**Серверы:**
- Мой сервер (владелец: user@vicord.dev)
- Геймеры (владелец: user@vicord.dev)
- Разработка (владелец: admin@vicord.dev)

## 🎮 Как пользоваться

### Первый запуск (Mock данные)

1. Откройте http://localhost:5173
2. Выберите сервер слева
3. Выберите канал
4. Начните отправлять сообщения!

**Примечание:** При первом запуске приложение будет использовать mock данные, так как пользователь не авторизован.

### С реальным Backend

Для работы с реальным API и базой данных, необходимо:

1. **Войти в систему** (функционал входа нужно добавить в UI)
2. **Или зарегистрироваться** через API:

```bash
# Регистрация через API
curl -X POST http://localhost:3001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "username": "testuser",
    "password": "password123"
  }'
```

3. Скопировать `accessToken` из ответа
4. Использовать токен в localStorage:

```javascript
// В консоли браузера (F12)
localStorage.setItem('accessToken', 'ваш_токен_здесь')
// Перезагрузить страницу
```

## 📚 Дополнительная информация

- **API документация:** см. `API.md`
- **Backend README:** см. `server/README.md`
- **TODO лист:** см. `TODO.md`

## 🐛 Troubleshooting

### Проблема: "Cannot connect to server"
**Решение:** Убедитесь, что backend запущен на порту 3001

### Проблема: "Database not found"
**Решение:** Запустите миграции: `cd server && npm run prisma:migrate`

### Проблема: "401 Unauthorized"
**Решение:** Авторизуйтесь через API или используйте тестовые данные

### Проблема: Сообщения не отправляются
**Решение:** 
1. Проверьте, что backend запущен
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что авторизованы (есть accessToken)

## 🎯 Следующие шаги

1. **Добавить UI для входа/регистрации** (в планах v0.3)
2. **Добавить создание серверов и каналов через UI**
3. **Добавить редактирование/удаление сообщений**
4. **Добавить индикаторы "печатает..."**

## 💡 Полезные команды

```bash
# Открыть Prisma Studio (GUI для БД)
cd server
npm run prisma:studio

# Пересоздать базу данных
cd server
rm dev.db
npm run prisma:migrate
npm run prisma:seed

# Проверить health backend
curl http://localhost:3001/health

# Получить список серверов (с токеном)
curl http://localhost:3001/api/servers \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

**Готово! Наслаждайтесь использованием ViCord! 🎉**

